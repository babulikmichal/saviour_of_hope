#include <iostream>
using namespace std;


int main () {

    int menu;
    string jmeno;
    int maxhp;
    int hp;
    int maxmp;
    int mp;
    int penize;
    int level = 1;
    int exp = 0;
    int atk;
    int schopnost;
    string cls;

    cout << "Vitejte v Saviour of Hope" << endl << "--------------------------------"<< endl;
    cout << "1) Hrat" << endl << "2) Ukoncit" << endl << "--------------------------------" << endl;
    cin >> menu;

    while(menu != 1 || menu != 2) {
        cout << "zadejte platnou hodnotu" << endl;
        cin >> menu;
    }
    
    if (menu == 2) {
        cout << "--------------------------------" << endl << "jste si jisti ze chcete kukoncit hru?" << endl << "1) Ukoncit" << endl << "2) Zpet" << endl << "--------------------------------" << endl;
        cin >> menu;

        while(menu != 1 || menu != 2) {
        cout << "zadejte platnou hodnotu" << endl;
        cin >> menu;
    }

    if (menu == 1) {
            return 0;
        }
    } else if(menu == 1) {

        cout << "--------------------------------" << endl << "Vyberte si jednu z class" << endl;
        cout << "1) Conquer" << endl << "2) Arch" << endl << "3) Persuader" << endl << "4) Abomination" << endl << "--------------------------------" << endl;
        cin >> menu;

        while(menu < 1 || menu > 4) {
        cout << "zadejte platnou hodnotu" << endl;
        cin >> menu;
    }
    

    
    
}